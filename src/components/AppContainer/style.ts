import styled from 'styled-components';

const AppContainer = styled.div`
  width: 100%;
  min-height: 100vh;
  background-color: #dedede;
  overflow-x: hidden;
  position: relative;
`;

export const Styles = {
  AppContainer,
};
