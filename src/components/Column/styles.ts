import styled from 'styled-components';

const Column = styled.div.attrs({})<{}>`
  display: flex;
  flex-direction: column;
  width: 100%;
  height: fit-content;
`;

const Styles = {
  Column,
};

export { Styles };
